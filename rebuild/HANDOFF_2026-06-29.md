# Phase 0 Handoff — fix-before-daily-use (2026-06-29)

**Branch:** `backend/workflow-series-3` (primary checkout; no worktrees — see `[[parallel-lane-model]]`).
**Source of truth:** `MASTER_PLAN.md` §23. Full audit: `ERP_AUDIT_2026-06-29.md`.
**Goal:** clear the 8 CONFIRMED defects blocking safe daily counter use. All are Low difficulty / High value.

## Conventions every lane must follow
- **Test runner:** `python -m pytest tests/test_s22_<lane>.py -q` (in-memory isolated DB per module; `JAKS_SKIP_AUTH=1` is auto-set by `tests/conftest.py`). Each lane adds ONE new test file `tests/test_s22_<lane>.py`.
- **Schema changes go via Alembic** (`app/migrations/versions/`, head is `0007`) **AND** the inline mirror in `app/database.py` (`_PENDING_COLUMN_ADDITIONS` / index-creation block) — see the existing 0002–0007 entries for the pattern. The model is the schema for fresh/test DBs (`create_all`); the migration + inline mirror cover pre-existing live DBs.
- **Role gating:** services use `self.assert_can(Permission.X)` (`app/services/base.py`, matrix `_ROLE_PERMISSIONS` lines 62-88). Route-level role gates use a `Depends(...)` like `require_admin` (`app/deps.py:92`). Roles: `ADMIN` (all perms), `BOOKKEEPING`, `SALES`, `READ_ONLY` (`constants.py:UserRole`).
- **Do NOT** run the full suite or any file-DB test concurrently — run only your own `test_s22_*` file. The orchestrator runs the full suite at integration.
- **Do NOT** `git add`/`commit`/`stash` — the orchestrator integrates and commits.
- **Stay in your file set.** Lanes are file-disjoint by design so they run in parallel safely.

## DO NOT re-litigate (verified-REFUTED — see §23.1)
CSRF (done) · core-charge customer≥vendor guard (done) · NSF-DRAFT money loss (not real) · Alembic silent-swallow (mirrored) · `test_phase2_seams` xfail (already fixed) · inventory cache "drift" (not a live bug).

---

## Lane A — Reports + Dashboard role gate  *(Risk #1, HIGH)*
**Files:** `app/deps.py`, `app/routers/reports.py`, `app/routers/dashboard.py`, `tests/test_s22_reports_authz.py`
- Add a dependency in `deps.py` mirroring `require_admin` (`deps.py:92`): `require_reports_access` that allows **ADMIN or BOOKKEEPING** (resolve via `get_current_user`, 403 otherwise). Honor the test bypass the same way `require_admin` does.
- Apply it to **every** route in `reports.py` (all reports expose cost basis / AR / competitor data) and to the dashboard route (`dashboard.py:27`). CSV export routes get the same gate.
- **Acceptance:** a SALES user → 403 on `/reports/ar-aging`, `/reports/inventory-valuation`, `/reports/lost-sales`, a CSV export, and `/dashboard`; an ADMIN and a BOOKKEEPING user → 200. (Use a real seeded user per role; the RBAC tests in the repo show the pattern of un-setting `JAKS_SKIP_AUTH` for role enforcement.)

## Lane B — Vendor name + code uniqueness  *(Risk #2, HIGH)*
**Files:** `app/models/vendor.py`, `app/routers/vendors.py`, `app/migrations/versions/0008_vendor_uniqueness.py`, `app/database.py`, `tests/test_s22_vendor_uniqueness.py`
- Model: add `__table_args__` with a **partial unique index** on `name` (`WHERE is_active = 1`) and a unique index on `vendor_code` (`WHERE vendor_code != ''`). SQLite supports partial indexes.
- Router: add a duplicate probe in **both** create paths in `vendors.py` (full create + quick-create) — case-insensitive name match and exact `vendor_code` match among active vendors → return a clear validation error, do not insert.
- Migration `0008` (down_revision `0007`): create the two unique indexes. Mirror them in `app/database.py`'s inline index-creation block with a comment naming revision 0008 (so a swallowed Alembic run still applies them on pre-existing DBs).
- **Acceptance:** creating a second active "PAI" (any case) is rejected at the router AND violates the DB index; a second vendor with the same non-empty `vendor_code` is rejected; two vendors with empty `vendor_code` are allowed.

## Lane C — Sales-order money correctness  *(Risks #3 + #4, HIGH)*
**Files:** `app/models/quote.py` (SalesOrder class — same file as Quote), `app/services/sales_order_service.py`, `app/templates/sales_orders/print.html`, `tests/test_s22_so_money.py`
- **Tax/total:** add `tax_amount` and `total` properties to **SalesOrder** mirroring Quote's (`taxable_base`/`tax_amount`/`total` already exist on Quote in this file — copy the shape, using `is_taxable` + `tax_rate_snapshot`). Do NOT touch the Quote class.
- **FULL gate:** in `fulfill_and_invoice` change `so_value = so.subtotal` (≈`sales_order_service.py:458`) to `so_value = so.total` so the deposit must cover subtotal **+ tax** before fulfillment. Keep the existing message; show the tax-inclusive figure.
- **Cancelled lines:** in `cancel_line` (`:265`) set `line.qty_cancelled = (line.qty_ordered - line.qty_invoiced)` **before** freezing `line.qty_ordered = line.qty_invoiced` (so the cancelled portion is recorded), and do the same on the cascaded children. `print.html:326` already skips fully-cancelled lines and `:342` shows a "cancelled" tag — once `qty_cancelled` is set, both work. (Optional: also guard on `line.line_status == 'cancelled'` if that field is authoritative.)
- **Acceptance:** a taxable FULL-mode SO with deposit == subtotal (but < subtotal+tax) is **blocked**; with deposit == total it **fulfills**. A line cancelled before any invoicing does **not** print as a live row (skipped or marked "cancelled"); a partially-invoiced-then-cancelled line prints the invoiced qty with a "N cancelled" tag.

## Lane D — Vendor bills route through AP  *(Risk #5, HIGH)*
**Files:** `app/services/po_service.py`, `app/routers/purchase_orders.py`, the bills template (`app/templates/purchase_orders/` — bills list/detail), `tests/test_s22_bill_approval.py`
- In `create_vendor_bill` (clean-match branch ≈`po_service.py:867`), default status to **PENDING** instead of APPROVED (a clean match still requires a human Approve). Keep DISCREPANCY behavior unchanged.
- Ensure `approve_bill` (`:1108`) transitions PENDING → APPROVED and is reachable from a UI **Approve** button on PENDING bills (gate the action on `Permission.APPROVE_VENDOR_BILL`, which BOOKKEEPING+ADMIN already have — `base.py:68`).
- **Acceptance:** creating a bill that matches the PO exactly lands **PENDING**; the Approve action moves it to APPROVED and makes it QBO-eligible; a SALES user cannot approve.

## Lane E — QBO transaction dates  *(Risk #6, HIGH)*
**Files:** `app/services/qbo_service.py`, `tests/test_s22_qbo_txndate.py`
- In `_build_invoice_payload` (`:770`) add `payload["TxnDate"] = inv.<finalized/created date>.strftime("%Y-%m-%d")` — use the invoice's authoritative finalize/issue date. In `_build_payment_payload` (`:352`) add `TxnDate` from the payment date. Mirror the **proven** vendor-bill pattern at `:498` (`payload["TxnDate"] = bill.bill_date.strftime("%Y-%m-%d")`).
- **Acceptance:** built invoice + payment payloads contain `TxnDate` equal to the transaction date (not "today"); assert the formatted string for a fixture invoice dated in a prior month.

## Lane F — Import category guard  *(Risk #7, HIGH)*
**Files:** `app/services/product_import_service.py`, `tests/test_s22_import_category_guard.py`
- In `_resolve_category` on the **direct** `full_import` path (≈`:1717-1732`), do not auto-create arbitrary level-1 categories from the scraper Type column. Refuse non-allowlisted names → leave the product uncategorized / flagged `needs_review` (mirror how Smart Import sets `category_issue`). The allowlist = existing top-level categories (and/or a seeded set). Surface the count of skipped/flagged categories in the import summary.
- **Acceptance:** a feed with Type "Misc"/"Test"/"Cat 3406 Head Gasket" creates **no** new top-level categories; affected rows are flagged `needs_review`; a feed whose Type matches an existing category still classifies normally.

## Lane G — Secrets / launch hardening  *(Risk #8 / #14, MED but pre-employee blocker)*
**Files:** `Start JAKS ERP.bat`, `Start JAKS ERP (LAN).bat`, `START JAKS.bat`, `app/main.py`, (a small template if needed), `tests/test_s22_password_rotation.py`
- Remove the `Login: admin / admin` echo from the launchers; set `JAKS_ENV=production` in both real launchers (`Start JAKS ERP.bat`, `Start JAKS ERP (LAN).bat`).
- Promote `_warn_if_default_admin_password` (`main.py:330`, currently a log-only warning) into a **non-dismissable redirect gate**: when a logged-in user's account still verifies the default password, force them to the password-change screen before any other navigation (middleware or a dependency on the app's authenticated routes). Apply to both admin and bookkeeper defaults.
- **Acceptance:** with the default password unchanged, every authenticated route redirects to the change-password flow until the password is rotated; after rotation, normal navigation resumes; the launchers no longer print credentials and export `JAKS_ENV=production`.

---

## Integration protocol (orchestrator)
1. Lanes A–G implement + write `tests/test_s22_<lane>.py` and self-run only their own test file.
2. Orchestrator runs the **full** suite: `python -m pytest -q` — expect prior baseline (~12 known reds per §21.4) unchanged, plus 7 new green files.
3. Resolve any cross-lane integration issues (none expected — file-disjoint).
4. Report the diff; **commit only on owner approval.**

## Why Phase 1+ is NOT parallel with Phase 0
Phase 1 items depend on Phase 0 landing (e.g. competitor-search index assumes the auth gate; cross-vendor dedup builds on stable vendor uniqueness) and on a 60–90-day daily-use trial. Run Phase 0 → verify → daily use → then Phase 1. See `MASTER_PLAN.md` §23.3.
