[pytest]
markers =
    visual: pixel-accurate visual regression tests (requires Playwright + live server)
    smoke: end-to-end Playwright workflow smoke suite (heavy; set JAKS_RUN_SMOKE=1 to run)
    acceptance: §8 cross-workflow acceptance flows (owner validation before go-live; fast, in-memory DB)
